node{
 try{
      stage('airsonic-adv') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'airsonic-adv')
      ]
}
 }    
   catch (err){
      echo "Airsonic Parameter fail"
   }
 try{
      stage('calibre-web') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'calibre-web')
      ]
}
 }    
   catch (err){
      echo "Calibre Web Parameter fail"
   }
 try{
      stage('ddclient') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'ddclient')
      ]
}
 }    
   catch (err){
      echo "ddclient Parameter fail"
   }
 try{
      stage('diun') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'diun')
      ]
}
 }    
   catch (err){
      echo "Diun Parameter fail"
   }
 try{
      stage('heimdall') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'heimdall')
      ]
}
 }    
   catch (err){
      echo "Heimdall Parameter fail"
   }
 try{
      stage('komga') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'komga')
      ]
}
 }    
   catch (err){
      echo "Komga Parameter fail"
   }
 try{
      stage('gitea') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'gitea')
      ]
}
 }    
   catch (err){
      echo "Gitea Parameter fail"
   }
 try{
      stage('nc-database') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'nc-database')
      ]
}
 }    
   catch (err){
      echo "NC-Database Parameter fail"
   }
 try{
      stage('phpmyadmin') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'phpmyadmin')
      ]
}
 }    
   catch (err){
      echo "PHPMyAdmin Parameter fail"
   }
 try{
      stage('plex') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'plex')
      ]
}
 }    
   catch (err){
      echo "Plex Parameter fail"
   }
 try{
      stage('qbittorrentvpn') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'qbittorrentvpn')
      ]
}
 }    
   catch (err){
      echo "QBittorrent Parameter fail"
   }
 try{
      stage('swag') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'swag')
      ]
}
 }    
   catch (err){
      echo "Swag Parameter fail"
   }
 try{
      stage('tautulli') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'tautulli')
      ]
}
 }    
   catch (err){
      echo "Tautulli Parameter fail"
   }
 try{
      stage('wireguard-ui') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'wireguard-ui')
      ]
}
 }    
   catch (err){
      echo "Wireguard-UI Parameter fail"
   }
 try{
      stage('audiobookshelf') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'audiobookshelf')
      ]
}
 }    
   catch (err){
      echo "Audiobookshelf Parameter fail"
   }
 try{
      stage('musicbot') 
{
  build job: 'update-docker-app', parameters: 
      [
      string(name: 'dockerapp', value: 'musicbot')
      ]
}
 }    
   catch (err){
      echo "Musicbot Parameter fail"
   }
}
